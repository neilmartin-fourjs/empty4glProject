
## An empty folder for starting a Genero program

## Makefile

The Makefile should edited to change the program and form names, set the DB and add any
webcomponents etc.

The goal is the Makefile can build a MANIFEST, xcf and gar for deployment.

